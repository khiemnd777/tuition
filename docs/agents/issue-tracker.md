# Issue Tracker

No Git repository metadata is present in this local folder, so no remote issue tracker is configured here.

Default workflow:

- Treat the current Codex thread as the source of task context.
- If a local issue artifact is useful, ask first, then write it under `.scratch/issues/<slug>.md`.
- If this folder later becomes a GitHub or GitLab repository, update this file with the real issue workflow.
